apiVersion: v1
kind: Service
metadata:
  name: student-note-service
spec:
  type: LoadBalancer
  selector:
    app: student-note-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8099